spring:
  profiles:
    active: ${ENV}

  application:
    name: executor

  cloud:
    consul:
      host: consul
      port: 8500
      discovery:
        instance-id: ${spring.application.name}_${random.int}