spring:
  profiles:
    active: ${ENV}

  application:
    name: content

  cloud:
    consul:
      host: consul
      port: 8500
      discovery:
        enabled: false

logging:
  level:
    com:
      newestworld: debug