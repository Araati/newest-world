server:
  port: 8060

spring:
  cloud:
    consul:
      discovery:
        instance-id: ${spring.application.name}_${random.int}
        heartbeat:
          enabled: true
          ttl: 10s
    stream:
      default-binder: rabbit
  rabbitmq:
    host: ${RABBIT_MQ_HOST:localhost}