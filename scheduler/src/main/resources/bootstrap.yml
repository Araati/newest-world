#todo Тут смешение двух конфигов. Нужно разделить на обычный и dev. В остальных сервисах аналогично
spring:
  profiles:
    active: ${ENV}

  application:
    name: scheduler

  cloud:
    consul:
      host: consul
      port: 8500
      discovery:
        instance-id: ${spring.application.name}_${random.int}